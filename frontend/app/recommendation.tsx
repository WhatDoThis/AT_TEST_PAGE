/**
 * app/recommendation.tsx (라우트 래퍼 — /recommendation)
 * ================================================================================
 * Expo Router 라우트는 app/ 하위에 있어야 하므로, 실제 화면 구현은
 * adobe_frontend/target-native-frontend/RecommendationScreen 으로 두고 여기서 default 만 re-export 한다.
 *
 * [Dependencies]
 * =========
 * - @adobe-native/RecommendationScreen
 */

export { default } from "@adobe-native/RecommendationScreen";
